{% extends "base.html.j2" %}
{% block title %}brand3 :: {{ domain }}{% endblock %}
{% block content %}
<section>
  <div class="section-head">
    <span class="label">brand_history · {{ domain }}</span>
    <span class="tag">// {{ analyses|length }} runs</span>
  </div>

  {% if analyses %}
  <div class="table-wrap">
  <table>
    <thead>
      <tr>
        <th class="col-date">date</th>
        <th class="num col-score">score</th>
        <th class="col-bar-wide">bar</th>
        <th class="col-band-wide">band</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
    {% for row in analyses %}
      <tr>
        <td>{{ row.date }}</td>
        <td class="num num-inline">{{ row.score_display }}</td>
        <td class="bar">{{ row.bar }}</td>
        <td class="dim">{{ row.band }}</td>
        <td><a href="/r/{{ row.token }}">view</a></td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  </div>
  {% if analyses|length > 1 %}
  <p class="small dim block-note">// evolution: older runs → newer runs read top-down</p>
  {% endif %}
  <p class="small dim block-note">
    own this brand? <a href="/takedown?domain={{ domain }}">request takedown →</a>
  </p>
  {% else %}
  <div class="empty">// no public analyses for this brand.</div>
  <p class="small dim block-note"><a href="/">← run the first analysis</a></p>
  {% endif %}
</section>
{% endblock %}
